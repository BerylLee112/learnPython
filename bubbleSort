def bubbleSort(nums):
    for j in xrange(len(numbers),-1,-1):
	    for i in xrange(0,j-1,1):
		    if nums[i] > nums[i+1]:
			    nums[i],nums[i+1] = nums[i+1],nums[i]

numbers = [[9,23,12,32,12],['b','w','u'],['22','81','3','666']]
for num in numbers:
    bubbleSort(num)
    print num

